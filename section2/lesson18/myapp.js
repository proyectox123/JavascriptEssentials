var array = [
	"String",
	100,
	[ "embed", 200 ], 
	{ car: "Ford" },
	function(){ return "Drive"; }
];

//array.shift(); // Remove first element
//array.pop(); // Remove last element
//array.unshift(0.1, 0.2, 0.3, null); // Add elements at the top
//array.push(0.1, 0.2, 0.3, null); // Add elements at the bottom
//array.splice( 2 , 2); // Remove elements from a position
//array.splice( 2 , 0, ...); // Added elements from a position
//array.splice( 2 , 2, ...); // Replace elements from a position